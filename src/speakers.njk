---
layout: layout
title: PCD India
---

<div class="grid grid-cols-5 grid-rows-layout gap-x-0 gap-y-4 justify-center">
  {% include "partials/nav.njk" %}

  <div class="col-span-full py-4">
    <div class="grid grid-cols-1 lg:grid-cols-2 lg:grid-cols-2 gap-x-4 gap-y-8">
      {% for speaker in speakers %}
        <div class="grid grid-cols-speaker
                    md:grid-rows-speaker-md md:grid-cols-speaker-md gap-x-4 items-start
                    shadow rounded py-6 px-4">
          <div class="font-light text-lg uppercase row-start-2"> {{ speaker.name }} </div>
          <div class="speaker-image col-start-2 col-span-2 md:col-start-2 md:col-span-1 row-span-full rounded-full">
            {% image "src/"+speaker.image, speaker.name, "(min-width: 30em) 50vw, 100vw" %}
          </div>
          <div class="text-xs col-span-2 md:col-span-1 row-start-3"> {{ speaker.intro }} </div>
          <div class="row-start-4 md:col-start-1 col-span-2 text-xs flex gap-2">
            {% for link in speaker.links %}
              <a class="text-blue-400 underline" href={{link.url}}> {{ link.text }} </a>
            {% endfor %}
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</div>


